# Ghostline-Studios-Website

The repo for the website — Next.js (App Router), React, and Tailwind CSS v4. Run `npm install` then `npm run dev` for local development. Deploy on Vercel by importing the repo (Next.js is auto-detected).

